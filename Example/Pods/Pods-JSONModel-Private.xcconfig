#include "Pods-JSONModel.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/JSONModel" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/JSONModel"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}